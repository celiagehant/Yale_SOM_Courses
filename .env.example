# Copy to .env (here in Lecture 7/, or one level up) and fill in your real key.
# Never commit the real .env.
PORTKEY_API_KEY=pk-your-portkey-key-here

# Optional: point the React app at a different backend origin.
# VITE_API_BASE=http://127.0.0.1:8000
